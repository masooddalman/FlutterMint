name: flutterforge
description: A CLI tool to generate Flutter projects with pre-configured architecture and modules.
version: 0.1.0

environment:
  sdk: ^3.0.0

executables:
  flutterforge: flutterforge

dependencies:
  args: ^2.6.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
